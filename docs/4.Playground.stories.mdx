import { Playground } from './Playground';
import * as UI from '../src';

export const name = 'Design System / Playground';

<Meta title={name} />

# Playground

All of the components are available in the playground!

<UI.Card css={`width: 200px;`}>
    <UI.Box p={4}>
        <UI.Typography>
            <UI.FadedScroll maxHeight={50} >
            {Object.keys(UI).sort()?.map(c => (
                <React.Fragment><UI.ListItem key={c}>{c}</UI.ListItem>
                <UI.Separator/></React.Fragment>
            ))}
            </UI.FadedScroll>
    </UI.Typography>
    </UI.Box>
</UI.Card>
<UI.Box mb={4}/>

<Playground code={`<Button>Hello world!</Button>`} />
