import React from 'react';
import { BadgeComponent } from './Badge.types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const Badge: BadgeComponent = ({ size, children }) => {
  return <div>{children}</div>;
};
