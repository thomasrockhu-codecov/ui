import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { FadedScroll } from './FadedScroll.tsx';

# FadedScroll

This component is a extions of CardWithTitle

## Usage

A vertical scrollbar will be present if height is set on either this component or a parent element while also the content (children prop) is higher than the set height.

## Props

<Props of={FadedScroll} />
