import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { IntlProvider } from 'react-intl';
import SegmentedControl from './SegmentedControl';
import { Number } from '../..';

<Meta title="Molecules | SegmentedControl" component={SegmentedControl} />

# SegmentedControl

SegmentedControl is a component built using the `Flexbox` component that mimicks a radio group with radio inputs.
Always uses 100% of the availble width.

<Canvas>
  <SegmentedControl>
    <SegmentedControl.Item itemId={0}>%</SegmentedControl.Item>
    <SegmentedControl.Item itemId={1}>SEK</SegmentedControl.Item>
  </SegmentedControl>
</Canvas>
