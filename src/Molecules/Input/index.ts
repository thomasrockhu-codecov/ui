import { Text } from './Text';
import { Number } from './Number';
import { Checkbox } from './Checkbox';

// Stitch all inputs so
// we can use e.g. Input.Text
export default { Text, Number, Checkbox };
