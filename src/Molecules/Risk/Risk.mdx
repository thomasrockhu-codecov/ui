import { Props } from '@storybook/addon-docs/blocks';
import { Playground } from '../../../docs/Playground';
import { Risk } from './Risk';

# Risk

This component will render a row of 7 rectangles with one colored rectangle containing. The colored rectangle contains a number from 1 - 7 depending on its position.

## Props

<Props of={Risk} />

## Usage

Basic usage

```javascript
<Risk risk={3} />
```

## Playground

<Playground code={`<Risk risk={3} />`} />
